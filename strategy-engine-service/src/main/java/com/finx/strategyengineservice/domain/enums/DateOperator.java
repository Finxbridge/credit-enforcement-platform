package com.finx.strategyengineservice.domain.enums;

/**
 * Operators for date filters
 */
public enum DateOperator {
    OLDER_THAN,      // Date is older than specified date
    NEWER_THAN,      // Date is newer than specified date
    INTERVAL         // Date is between two dates
}
